import React from 'react';
import SearchForm from '../SearchForm/SearchForm';

const Main = () => {
    return (
        <main className="main">
            <SearchForm />
        </main>
    );
};

export default Main;